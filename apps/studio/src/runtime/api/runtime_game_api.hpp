#pragma once

// Compatibility include for existing Studio sources. The runtime interface is
// engine-public because project DLLs and every host share this exact contract.
#include "engine/game_runtime_api.hpp"
